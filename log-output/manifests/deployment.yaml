apiVersion: apps/v1
kind: Deployment
metadata:
  name: log-output
spec:
  replicas: 1
  selector:
    matchLabels:
      app: log-output
  template:
    metadata:
      labels:
        app: log-output
    spec:
      volumes:
        - name: shared-image
          emptyDir: {}
      containers:
        - name: log-writer
          image: eplol/log-writer:0.3
          volumeMounts:
          - name: shared-image
            mountPath: /mydir/files
          env:
          - name: "PORT"
            value: "3000"
        - name: log-getter
          image: eplol/log-getter:0.1
          volumeMounts:
          - name: shared-image
            mountPath: /mydir/files
          env:
          - name: "PORT"
            value: "3000"